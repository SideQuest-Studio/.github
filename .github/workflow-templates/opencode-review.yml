name: OpenCode Review

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  review:
    if: github.event.pull_request.head.repo.fork == false
    uses: SideQuest-Studio/.github/.github/workflows/opencode-pr.yml@main
    secrets: inherit
