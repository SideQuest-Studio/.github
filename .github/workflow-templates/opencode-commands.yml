name: OpenCode Commands

on:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]

jobs:
  opencode:
    uses: SideQuest-Studio/.github/.github/workflows/opencode.yml@main
    secrets: inherit
